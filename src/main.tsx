import React from 'react'
import ReactDOM from 'react-dom/client'
import ButtonType from '../components/button/demo/button-type'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
	<React.StrictMode>
		<ButtonType />
	</React.StrictMode>
)
